<%= form_for(assignment) do |f| %>
  <% if assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assignment.errors.count, "error") %> prohibited this assignment from being saved:</h2>

      <ul>
      <% assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :zombie_id %>
    <%= f.select :zombie_id, Zombie.all.collect { |a| [a.name, a.id]} %>
  </div>

  <div class="field">
    <%= f.label :role_id %>
    <%= f.select :role_id, Role.all.collect { |b| [b.title, b.id] } %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
